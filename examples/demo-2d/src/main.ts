/**
 * demo-2d — top-down shooter test game (BRAINSTORM.md §6.4).
 * Mục tiêu đầu tiên: 1 box di chuyển với prediction + reconciliation,
 * đo misprediction dưới lag giả lập (proxy delay).
 */
export {};
