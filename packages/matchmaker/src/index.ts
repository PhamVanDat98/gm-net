/**
 * @gm-net/matchmaker — optional service (Phase 3): queue, server registry
 * qua Redis, REST/WS endpoint riêng. Stateless, tách khỏi core — framework
 * dùng được mà không cần matchmaker. Dependency redis thêm khi bắt đầu.
 */
export {};
