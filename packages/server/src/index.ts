/**
 * @gm-net/server — room framework trên Colyseus (room lifecycle + seat
 * reservation, bypass schema sync — snapshot binary gửi qua sendBytes),
 * fixed-tick loop, lag compensation, AOI.
 *
 * Phase 1: tick loop + input buffer + snapshot broadcast.
 */
export {};
